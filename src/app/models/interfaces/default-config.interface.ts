export interface filterConfig {
  createdAt: string;
  quantity: number;
  price: number;
  search: string;
  priceSelect: string;
  dateSelect: string;
  quantitySelect: string;
  sortFrom: string;
  sort: string;
}
